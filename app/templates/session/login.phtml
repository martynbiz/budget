<?php $this->layout('layouts/main', [
    'title' => 'Budget',
]); ?>

<?php $this->section('content') ?>
        <div class="row">
            <div class="small-12 columns">
                <form id="login_form" method="post" action="<?= $this->pathFor('login_post') ?>">
                    <label><?= $this->translate('username_label') ?>
                        <input type="text" name="email" placeholder="<?= $this->translate('username_label') ?>" value="<?= @$this->params['email'] ?>">
                    </label>

                    <label><?= $this->translate('password_label') ?>
                        <input  type="password" class="form-control" name="password" placeholder="<?= $this->translate('password_label') ?>" value="">
                    </label>

                    <label><input type="checkbox" name="remember_me" value="1" checked="checked"> <?= $this->translate('remember_me_text') ?></label>

                    <button class="button" type="submit">
                        <?= $this->translate('login_button') ?>
                    </button>

                    <p><a href="<?= $this->pathFor('register') ?>"><?= $this->translate('register_button') ?></a><br>
                    <a href="/accounts/resetpassword"><?= $this->translate('forgot_password_link') ?></a></p>

                    <?php if(isset($this->params['returnTo'])): ?>
                        <input type="hidden" name="returnTo" value="<?= $this->params['returnTo'] ?>">
                    <?php endif ?>
                </form>
            </div>
        </div>
<?php $this->replace() ?>
