<?php $this->layout('layouts/main'); ?>

<?php $this->section('content') ?>
        <div class="row">
            <div class="small-12 columns">
                <form id="login_form" method="post" action="<?= $this->pathFor('login_post') ?>">
                    <label><?= $this->translate('username') ?>
                        <input type="text" name="email" placeholder="<?= $this->translate('username') ?>" value="<?= @$this->params['email'] ?>">
                    </label>

                    <label><?= $this->translate('password') ?>
                        <input  type="password" class="form-control" name="password" placeholder="<?= $this->translate('password') ?>" value="">
                    </label>

                    <label><input type="checkbox" name="remember_me" value="1" checked="checked"> <?= $this->translate('remember_me') ?></label>

                    <button class="button" type="submit">
                        <?= $this->translate('login_button') ?>
                    </button>

                    <p><a href="<?= $this->pathFor('register') ?>"><?= $this->translate('register_button') ?></a><br>
                    <a href="/accounts/resetpassword"><?= $this->translate('forgot_password') ?></a></p>

                    <?php if(isset($this->params['returnTo'])): ?>
                        <input type="hidden" name="returnTo" value="<?= $this->params['returnTo'] ?>">
                    <?php endif ?>
                </form>
            </div>
        </div>
<?php $this->replace() ?>
