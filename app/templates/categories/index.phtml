<?php $this->layout('layouts/main', [
    'title' => 'Budget',
]); ?>

<?php $this->section('content') ?>
    <div class="rows">
        <div class="small-12 medium-offset-8 medium-2 columns text-right">
            <a href="<?= $this->pathFor('categories_create') ?>" class="button expanded">New category</a>
        </div>
        <div class="small-12 medium-2 columns text-right">
            <a href="<?= $this->pathFor('categories_create') ?>" class="button expanded">New group</a>
        </div>
    </div>

    <div class="row">
        <div class="small-12 medium-12 columns">
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th width="15%">Group</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach($this->categories as $category): ?>
                        <tr>
                            <td><a href="<?= $this->pathFor('categories_edit', ['category_id' => 1]) ?>"><?= $category->name ?></a></td>
                            <td><?= $category->category_group->name ?></td>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>

            <?= $this->insert('partials/pagination') ?>
        </div>
    </div>
<?php $this->replace() ?>
