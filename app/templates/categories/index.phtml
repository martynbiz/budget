<?php $this->layout('layouts/main', [
    'title' => 'Budget',
]); ?>

<?php $this->section('content') ?>
    <div class="rows">
        <div class="small-12 medium-offset-10 medium-2 columns text-right">
            <a href="<?= $this->pathFor('categories_create') ?>" class="button expanded">
                <i class="fa fa-plus" aria-hidden="true"></i>
                <?= $this->translate('new_category_button') ?>
            </a>
        </div>
    </div>

    <div class="row">
        <div class="small-12 medium-12 columns">
            <table>
                <thead>
                    <tr>
                        <th><?= $this->translate('name_header') ?></th>
                        <th width="15%"><?= $this->translate('group_header') ?></th>
                        <th width="15%"><?= $this->translate('balance_header') ?></th>
                        <th width="15%"><?= $this->translate('budget_remaining_header') ?></th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach($this->categories as $category): ?>
                        <tr>
                            <td><a href="<?= $this->pathFor('categories_edit', ['category_id' => $category->id]) ?>"><?= (($category->parent_id > 0) ? '- ' : '') . $category->name ?></a></td>
                            <td><?= $category->group->name ?></td>
                            <td><?= sprintf($this->current_fund->currency->format, $category->amount) ?></td>
                            <td class="<?= ($category->budget > 0) ? (($category->balance > 0) ? 'balance-in' : (($category->balance < 0) ? 'balance-out' : 'balance-even')) : '' ?>"><?= ($category->budget > 0) ? sprintf($this->current_fund->currency->format, $category->balance) : '' ?></td>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>

            <?= $this->insert('partials/pagination') ?>
        </div>
    </div>
<?php $this->replace() ?>
